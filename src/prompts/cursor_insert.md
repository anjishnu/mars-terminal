

CURSOR — {file} ({lang}), line {line} (nothing selected). If the user asks you to write, generate, or produce something, reply with ONLY the text to insert at the cursor — valid {lang}, matching the surrounding indentation — inside one ``` code block and no prose.
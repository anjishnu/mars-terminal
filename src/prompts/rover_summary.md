Below is the raw recent output of ONE workspace on the captain's machine. Summarize it in a SINGLE line (two short clauses at most) for a phone glance.

INTERPRET the output into plain human language — do not recite raw numbers, step indices, config names, or metrics the captain can't read at a glance. Say "the training run is about two-thirds through" or "the build finished clean", not "epoch 7/10, loss 0.42". Keep a genuinely meaningful term (a named test, an error type, a file path) where a vaguer word would lose the point.

If the workspace is blocked or failing, LEAD with the single next action the captain should take. If it's running fine, say what it's doing and roughly how far along. If it just finished a real piece of work, treat it as the win it is. A dry beat of wit is fine when it's going well; drop it the moment something is on fire.

One line. No markdown, no preamble like "this workspace", no trailing punctuation beyond a period.

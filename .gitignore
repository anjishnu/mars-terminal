# Generated by Cargo
target/
**/*.rs.bk
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Testing
coverage/
*.profraw
*.profdata

# Build artifacts
*.o
*.a
*.so
*.dylib
*.dll
*.lib
*.exe

# Logs
*.log

# Environment
.env
.env.local
# Forward-looking product strategy stays private unless a reviewed handoff is
# explicitly allow-listed here.
# Forward-looking product strategy stays private — the whole directory, no exceptions. The two
# files that were previously un-ignored (README.md, windows-parity-handoff.md) went with it: one
# carve-out is how a directory that is meant to be private starts leaking again.
design_ideas/
# Research artifacts (EMNLP paper + experiment harness/results) — they live on
# the naacl-research branch and local disk, not in the product mainline
paper/
eval/
final_results.md

# Python
__pycache__/
*.pyc


.claude/worktrees/
